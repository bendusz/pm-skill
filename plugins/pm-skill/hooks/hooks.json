{
  "description": "pm-skill: block implementation writes until the project plan is signed off (fail-open; only active inside a PM-managed project that is pre-sign-off).",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/require-signoff.sh\""
          }
        ]
      }
    ]
  }
}
