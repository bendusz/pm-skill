{
  "description": "pm-skill guardrails (all fail-open, kill switch PM_SKILL_NO_ENFORCE=1): block implementation writes until the plan is signed off, block secret-shaped content from entering the git-tracked pm/ state directory, and re-ground new/compacted sessions from pm/ state, and block edits to another actor's pm/actors/ state files.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/session-context.sh\""
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/require-signoff.sh\""
          },
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/pm-secrets-guard.sh\""
          },
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/actor-guard.sh\""
          }
        ]
      }
    ]
  }
}
