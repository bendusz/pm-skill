# Plan Quality Checklist — <Project>
<!-- Generated to docs/checklists/plan-quality.md. Check against docs/plan.md (and docs/spec.md). -->

- [ ] The plan derives from `docs/spec.md` (requirements traced by ID).
- [ ] Scope is explicit — both In and Out.
- [ ] The Stories table has a `covers` column mapping each story to requirement IDs.
- [ ] Commands (test/lint/build/run) are real, or explicitly `N/A`.
- [ ] Each risk has a mitigation.
- [ ] Architecture decisions are stated and justified.
- [ ] Constitution constraints (`docs/constitution.md`) are addressed.
- [ ] A Sign-off line exists (filled only on explicit human approval).
- [ ] No unresolved `[NEEDS CLARIFICATION]` markers remain.
