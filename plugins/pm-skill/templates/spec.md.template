# <Project> — Feature Specification

<!--
Focus on WHAT users need and WHY — never HOW. No stack, no architecture; that belongs in docs/plan.md.
Use stable IDs (US-001, FR-001, AC-001, SM-001). Stories and the plan trace to FR-/AC- (US- is narrative context; SM- is outcome metrics, often non-buildable); never renumber an existing ID.
Every requirement must be testable, or explicitly marked as a non-buildable business metric.
Mark every unknown inline as [NEEDS CLARIFICATION: <question>] — do not guess.
-->

## Overview
<what users need and why, without implementation details>

## User stories
### US-001 — <title>
As a <user>, I want <capability>, so that <outcome>.

## Functional requirements
- FR-001: <testable requirement>

## Acceptance criteria
- AC-001: <observable / testable criterion>

## Edge cases
- <edge case or failure mode — and the expected behaviour>

## Success metrics
- SM-001: <measurable success signal; mark non-buildable business metrics as such>

## Assumptions
- <assumption>

## Clarifications
<!-- Must be empty before planning/sign-off. Use [NEEDS CLARIFICATION: ...] markers above until
     resolved (via /pm-skill:clarify); record each resolution here once answered. -->
